declare namespace _default {
    let library_init: string;
    let library_beforeInit: string;
    let file_beforeAdded: string;
    let file_afterAdded: string;
    let file_beforePassedChecks: string;
    let file_removed: string;
    let file_defaultFileRemoved: string;
    let file_all_removed: string;
    let upload_beforeStart: string;
    let upload_progress: string;
    let upload_success: string;
    let upload_error: string;
    let upload_retry: string;
    let upload_all_finished: string;
    let file_source_closed: string;
    let default_ui_shown: string;
    let default_ui_closed: string;
    let video_recordingStarted: string;
    let video_recording: string;
    let video_recordStop: string;
    let video_recordSaved: string;
    let video_recordCancel: string;
    let image_captured: string;
    let audio_recordingStarted: string;
    let audio_recording: string;
    let audio_recordStop: string;
    let audio_recordSaved: string;
    let audio_recordCancel: string;
    let screen_recordingStarted: string;
    let screen_recording: string;
    let screen_recordStop: string;
    let screen_recordSaved: string;
    let screen_recordCancel: string;
}
export default _default;
/**
 * Types of CustUp event types
 */
export type TEventNames = 'library.init' | 'library.beforeInit' | 'file.beforeAdded' | 'file.afterAdded' | 'file.beforePassedChecks' | 'file.removed' | 'file.defaultFileRemoved' | 'file.all_removed' | 'upload.beforeStart' | 'upload.progress' | 'upload.success' | 'upload.error' | 'upload.retry' | 'upload.all_finished' | 'file_source.closed' | 'default_ui.shown' | 'default_ui.closed' | 'video.recordingStarted' | 'video.recording' | 'video.recordStop' | 'video.recordSaved' | 'video.recordCancel' | 'image.captured' | 'audio.recordingStarted' | 'audio.recording' | 'audio.recordStop' | 'audio.recordSaved' | 'audio.recordCancel' | 'screen.recordingStarted' | 'screen.recording' | 'screen.recordStop' | 'screen.recordSaved' | 'screen.recordCancel';
//# sourceMappingURL=eventNames.d.ts.map